<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>board</title>
    <link rel="stylesheet" href="./board_style.css">
</head>
<body>
    <div class="board_wrap">
        <div class="board_title">
            <strong>게시판</strong>
            <p>글의 전체 목록</p>
        </div>
        <div class="board_list_wrap">
            <div class="board_list">
                <div class="top">
                    <div class="num">번호</div>
                    <div class="title">제목</div>
                    <div class="writer">글쓴이</div>
                    <div class="date">작성일</div>
                </div>
                <div>
                    <% for (var i = 1; i <= count; i++){ %>
                        <div class="num"><%= p_num+i %></div>
                        <div class="title"><a href=<%= "/update?title=" + row[0][i-1] + "?writer=" + row[1][i-1] + "?dt=" + row[3][i-1] %>><%= row[0][i-1] %></a></div>
                        <div class="writer"><%= row[1][i-1] %></div>
                        <div class="date"><%= row[3][i-1] %></div>
                    <%}%>
                </div>
            </div>
            <div class="board_page">
                <a href=<%= prev %>>이전페이지</a>
                <a href=<%= next %>>다음페이지</a>
            </div>
            </div>
        </div>        
    </div>
    <form action="/write_board">
        <input type="submit">
    </form>
</body>
</html> 